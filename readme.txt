# Networking
- Added /REVERSE command to the server commands so it will reverse the whole sentence order and word order
- Added line clearing after user inputs a message, they receive their message back to them with thier name from the server or if the message doesnt send they get an error and closes the client.